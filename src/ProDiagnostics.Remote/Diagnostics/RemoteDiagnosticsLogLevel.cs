namespace Avalonia.Diagnostics.Remote;

/// <summary>
/// Structured backend diagnostics logging severity.
/// </summary>
public enum RemoteDiagnosticsLogLevel
{
    Trace = 0,
    Debug = 1,
    Information = 2,
    Warning = 3,
    Error = 4,
    Critical = 5,
}
