<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="using:Avalonia.Diagnostics.Converters"
             xmlns:vm="using:Avalonia.Diagnostics.ViewModels"
             xmlns:models="using:Avalonia.Diagnostics.Models"
             x:Class="Avalonia.Diagnostics.Views.EventsPageView"
             Margin="2"
             x:DataType="vm:EventsPageViewModel">
  <UserControl.Resources>
    <conv:RoutingStrategyToBrushConverter x:Key="RoutingBrush" />
  </UserControl.Resources>

  <UserControl.Styles>
    <Style Selector="TextBlock.nav" >
      <Setter Property="TextDecorations">
        <TextDecorationCollection>
          <TextDecoration Location="Underline" Stroke="Black" StrokeThickness="1" StrokeDashArray="2,2"/>
        </TextDecorationCollection>
      </Setter>
    </Style>

    <Style Selector="TextBlock.nav:pointerover" >
      <Setter Property="Foreground" Value="{DynamicResource ThemeAccentBrush}" />
      <Setter Property="Cursor" Value="Help" />
    </Style>

    <Style Selector="ListBoxItem" >
      <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style Selector="ListBoxItem:selected /template/ ContentPresenter" >
      <Setter Property="BorderBrush" Value="Black" />
    </Style>

    <Style Selector="ListBoxItem.handled" >
      <Setter Property="Background" Value="#34c940" />
      <Setter Property="Foreground" Value="Black" />
    </Style>
  </UserControl.Styles>

  <Grid ColumnDefinitions="1.1*,4,3*">

    <Grid Grid.Column="0" RowDefinitions="Auto,*,Auto">

      <FilterTextBox Grid.Row="0" Margin="0,0,0,2"
                              DataContext="{Binding EventsFilter}"
                              Text="{Binding FilterString}"
                              Watermark="Filter events"
                              UseCaseSensitiveFilter="{Binding UseCaseSensitiveFilter}"
                              UseWholeWordFilter="{Binding UseWholeWordFilter}"
                              UseRegexFilter="{Binding UseRegexFilter}" />

      <TreeView Grid.Row="1" ItemsSource="{Binding Nodes}" SelectedItem="{Binding SelectedNode, Mode=TwoWay}" >
        <TreeView.DataTemplates>
          <TreeDataTemplate DataType="vm:EventTreeNodeBase"
                            ItemsSource="{Binding Children}">
            <CheckBox Content="{Binding Text}" IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
          </TreeDataTemplate>
        </TreeView.DataTemplates>
        <TreeView.Styles>
          <Style Selector="TreeViewItem" x:DataType="vm:EventTreeNodeBase">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="IsVisible" Value="{Binding IsVisible}" />
          </Style>
        </TreeView.Styles>
      </TreeView>

      <StackPanel Grid.Row="2" Margin="0,2" Orientation="Horizontal" Spacing="2">
        <Button Content="Disable all" Command="{Binding DisableAll}" />
        <Button Content="Enable default" Command="{Binding EnableDefault}" />
      </StackPanel>

    </Grid>

    <GridSplitter Width="4" Grid.Column="1" />

    <Grid RowDefinitions="*,4,2*,Auto,Auto" Grid.Column="2">

      <ListBox Name="EventsList" ItemsSource="{Binding RecordedEventsView}"
               SelectedItem="{Binding SelectedEvent, Mode=TwoWay}">
        <ListBox.Styles>
          <Style Selector="ListBoxItem">
            <Setter Property="Classes.handled" Value="{Binding IsHandled}" x:DataType="vm:FiredEvent" />
          </Style>
        </ListBox.Styles>

        <ListBox.ItemTemplate>
          <DataTemplate x:DataType="vm:FiredEvent">
            <Grid ColumnDefinitions="Auto,Auto,Auto,*,Auto">
              <Grid.ContextFlyout>
                <MenuFlyout>
                  <MenuItem Header="Set Breakpoint (Global)"
                            Command="{Binding $parent[UserControl].((vm:EventsPageViewModel)DataContext).AddGlobalEventBreakpoint}"
                            CommandParameter="{Binding}" />
                  <MenuItem Header="Set Breakpoint (On Source)"
                            Command="{Binding $parent[UserControl].((vm:EventsPageViewModel)DataContext).AddSourceEventBreakpoint}"
                            CommandParameter="{Binding}" />
                </MenuFlyout>
              </Grid.ContextFlyout>

              <TextBlock Grid.Column="0"  Text="{Binding TriggerTime, StringFormat={}{0:HH:mm:ss.fff}}"/>

              <StackPanel Margin="10,0,0,0" Grid.Column="1" Spacing="2" Orientation="Horizontal" >
                <TextBlock Tag="{Binding Event}" DoubleTapped="NavigateTo" Text="{Binding Event.Name}" FontWeight="Bold" Classes="nav" />
                <TextBlock Text="on" />
                <TextBlock Tag="{Binding Originator}" DoubleTapped="NavigateTo" Text="{Binding Originator.HandlerName}" Classes="nav" />
              </StackPanel>

              <StackPanel Margin="2,0,0,0" Grid.Column="2" Spacing="2" Orientation="Horizontal" IsVisible="{Binding IsHandled}" >
                <TextBlock Text="::" />
                <TextBlock Text="Handled by" />
                <TextBlock Tag="{Binding HandledBy}" DoubleTapped="NavigateTo" Text="{Binding HandledBy.HandlerName}" Classes="nav" />
              </StackPanel>

              <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Routing (" />
                <TextBlock Text="{Binding ObservedRoutes}"
                           Foreground="{Binding ObservedRoutes, Converter={StaticResource RoutingBrush}}"/>
                <TextBlock Text=")"/>
              </StackPanel>

            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <GridSplitter Height="4" Grid.Row="1" />

      <DockPanel Grid.Row="2" LastChildFill="True">
        <TextBlock DockPanel.Dock="Top" FontSize="16" Text="Event chain:" />

        <ListBox ItemsSource="{Binding SelectedEvent.EventChain}">
          <ListBox.Styles>
            <Style Selector="ListBoxItem">
              <Setter Property="Classes.handled" Value="{Binding Handled}" x:DataType="models:EventChainLink" />
            </Style>
          </ListBox.Styles>
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="models:EventChainLink">
              <StackPanel Orientation="Vertical"
                          Background="Transparent"
                          PointerEntered="ListBoxItem_PointerEntered"
                          PointerExited="ListBoxItem_PointerExited">
                <StackPanel.ContextFlyout>
                  <MenuFlyout>
                    <MenuItem Header="Set Breakpoint (On Handler)"
                              Command="{Binding $parent[UserControl].((vm:EventsPageViewModel)DataContext).AddChainEventBreakpoint}"
                              CommandParameter="{Binding}" />
                  </MenuFlyout>
                </StackPanel.ContextFlyout>

                <Rectangle IsVisible="{Binding BeginsNewRoute}" StrokeDashArray="2,2" StrokeThickness="1" Stroke="Gray" />

                <StackPanel Orientation="Horizontal" Spacing="2">
                  <TextBlock Text="{Binding Route}"
                             FontWeight="Bold"
                             Foreground="{Binding Route, Converter={StaticResource RoutingBrush}}" />
                  <TextBlock Tag="{Binding}"
                             DoubleTapped="NavigateTo"
                             Text="{Binding HandlerName}"
                             Classes="nav" />
                </StackPanel>

              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

      </DockPanel>

      <WrapPanel Orientation="Horizontal" Grid.Row="3" Margin="2" ItemHeight="24">
        <TextBlock Text="Filters:"
                   VerticalAlignment="Center"
                   Margin="0,0,4,0" />
        <CheckBox Content="Bubble"
                  IsChecked="{Binding IncludeBubbleRoutes}"
                  Margin="0,0,6,0" />
        <CheckBox Content="Tunnel"
                  IsChecked="{Binding IncludeTunnelRoutes}"
                  Margin="0,0,6,0" />
        <CheckBox Content="Direct"
                  IsChecked="{Binding IncludeDirectRoutes}"
                  Margin="0,0,6,0" />
        <CheckBox Content="Handled"
                  IsChecked="{Binding IncludeHandledEvents}"
                  Margin="8,0,6,0" />
        <CheckBox Content="Unhandled"
                  IsChecked="{Binding IncludeUnhandledEvents}"
                  Margin="0,0,6,0" />
        <CheckBox Content="Auto-scroll"
                  IsChecked="{Binding AutoScrollToLatest}"
                  Margin="8,0,8,0" />
        <TextBlock Text="{Binding VisibleRecordedEvents, StringFormat='Visible: {0}'}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0" />
        <TextBlock Text="{Binding TotalRecordedEvents, StringFormat='Total: {0}'}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0" />
      </WrapPanel>

      <StackPanel Orientation="Horizontal" Grid.Row="4" Spacing="2" Margin="2">
        <Button Content="Clear" Command="{Binding Clear}" />
      </StackPanel>

    </Grid>
  </Grid>
</UserControl>
