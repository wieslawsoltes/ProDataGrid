<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Avalonia.Diagnostics.ViewModels"
        xmlns:views="using:Avalonia.Diagnostics.Views"
        x:Class="Avalonia.Diagnostics.Views.ResourceReferencePickerWindow"
        x:DataType="vm:ResourceReferencePickerViewModel"
        Width="920"
        Height="620"
        MinWidth="720"
        MinHeight="480"
        Title="{Binding Title}">
  <views:ResourceReferencePickerView Name="PART_Picker"/>
</Window>
